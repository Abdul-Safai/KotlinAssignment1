# KotlinAssignment1

Implements:
- `sumUpTo(max: Int): Long`
- `factorialIterative(n: Int): Long`
- `factorialLambda: (Int) -> Long`
- `factorialRecursive(n: Int, acc: Long = 1L): Long` (tail-rec)

## Build & Run
- Gradle 8.14
- JDK 21

```bash
./gradlew clean build
./gradlew run

